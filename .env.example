# 单仓部署（code_demo 已合并进本仓库）：一个 Vercel 项目即可品牌站 + App + API + Prisma。
# 复制为 .env.local 后仅在本地生效（勿将 .env.local 提交到 Git）。
# Next.js 仅会把以 NEXT_PUBLIC_ 开头的变量打进浏览器端代码。
#
# 本地执行 npm run db:seed 时：prisma/seed.ts 会依次加载 .env 与 .env.local（后者覆盖前者）。
# 若你只建了 .env.local 并写了 DATABASE_URL，无需再建 .env；若仍报错，确认该行未被注释且连接串完整。

# --- 数据库（本地 / Render / Vercel 均使用 PostgreSQL）---
# 本地：复制本文件为 .env.local，填入 Neon / Render External URL / 本地 Docker Postgres。
# Render：在 Web Service → Environment 填入 Internal Database URL（见仓库根 render.yaml）。
# 未配置时 `npm run build` 仍会完成 Next 构建，但会跳过 prisma migrate deploy；
# 配好 DATABASE_URL 后请重新部署一次，让迁移在构建阶段执行。
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require
# DIRECT_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require
#   · 仅配 DATABASE_URL 也可部署：构建脚本会自动把 DIRECT_URL 设为与 DATABASE_URL 相同
#   · Render 自带 Postgres：两者填同一 Internal/External URL 即可（render.yaml 已自动注入）
#   · Neon：DATABASE_URL 用 pooler（带 -pooler 主机名），DIRECT_URL 用直连 host（不带 pooler）
#   · pooler URL 会自动补 ?pgbouncer=true（见 lib/prisma.ts）；仍 P2024 时可加 &connection_limit=5
#   · 本地开发若频繁 P2024：可临时把 DATABASE_URL 改为 DIRECT_URL 同款直连（非 pooler）
#
# --- 若 Vercel 构建报 P3009（migrate found failed migrations）---
# 原因多为：某次迁移在库里被记为 failed（例如旧 migration.sql 带 BOM 已失败）。
# 任选其一即可（开发库常见；BOM 已修复后需清掉失败状态才能再 deploy）：
# 1）本地：配置好 DATABASE_URL 后执行
#      npm run db:resolve-failed-init
#      npx prisma migrate deploy
# 2）Vercel：Environment Variables 临时新增 PRISMA_RESOLVE_ROLLED_BACK_INIT=1（值任意为 1），
#    Redeploy 成功一次后立刻删除该变量，避免以后误触。
# 若库里已误建出半套表，请在 Neon 控制台删库/清表后再 migrate deploy。

# --- 可选：演示种子 API（与 prisma/seed 或 /api/seed 说明一致）---
# DEMO_SEED_SECRET=your-long-random-secret

# --- 同源合并后：可不设，/login 内嵌会使用本站 /welcome/login?embed=1 ---
# 若 Vercel 里仍留着旧的 NEXT_PUBLIC_MINIAPP_URL 且指向「本站同一域名」，代码会视为同源合并；
# 为免混淆可直接删除该变量后 Redeploy。
# 若仍用「品牌站 + 独立 App 域名」分仓部署，再填写外域 App origin：
# NEXT_PUBLIC_MINIAPP_URL=https://你的-app.vercel.app

# --- 可选：/welcome/login 的 CSP frame-ancestors 额外允许列表（逗号分隔）---
# FRAME_ANCESTOR_ORIGINS=https://example.com

# --- 可选：canonical / 绝对链接（未设且部署在 Vercel 时 next.config 会用 VERCEL_URL 推断）---
# NEXT_PUBLIC_SITE_URL=https://你的项目.vercel.app

# --- 真实用户认证 ---
# SESSION_SECRET=至少32位随机字符串（可选；未设时 npm start / render-start 会自动生成）
# 固定值可避免 Render 重启后全员掉线；不配置也能正常登录访问
#
# --- Render 只上传本目录（code_demo_web）时 ---
# Root Directory 留空；Build = npm install && npm run build
# Start Command = node scripts/render-start.cjs
# 必填环境变量：DATABASE_URL（及 Neon 时的 DIRECT_URL）
# 可选：NEXT_PUBLIC_SITE_URL / APP_URL 设为线上域名
# RESEND_API_KEY=re_xxxx
# EMAIL_FROM=VibeCoding <noreply@你的已验证域名>
# APP_URL=https://founder-bridge-ai.onrender.com
# ENABLE_DEMO_LOGIN=false
# ENABLE_GUEST=false
