# 仓库策略：可与 code_demo（App 壳）分两个 GitHub 仓库；各自连一个 Vercel 项目即可。
# 复制为 .env.local 后仅在本地生效（勿将 .env.local 提交到 Git）。
# Next.js 仅会把以 NEXT_PUBLIC_ 开头的变量打进浏览器端代码。
#
# App 壳（code_demo）的完整 origin；网页端 /login 内嵌 iframe 会请求：
#   {NEXT_PUBLIC_MINIAPP_URL}/welcome/login?embed=1
#
# 本地：若未设置，开发模式会默认使用本机 App 常见端口（见 lib/miniappOrigin.ts）。
# 公网（Vercel 等）：必须在平台「Environment Variables」填写已部署 App 的 HTTPS 地址，例如：
# NEXT_PUBLIC_MINIAPP_URL=https://你的-app-壳项目.vercel.app
#
# 示例（本地指向本机运行的 code_demo，端口以你实际终端为准）：
# NEXT_PUBLIC_MINIAPP_URL=http://127.0.0.1:3000
#
# 品牌站 Vercel：Settings → Environment Variables 添加/修改后，务必 Redeploy 一次才生效。
