# 单仓部署（code_demo 已合并进本仓库）：一个 Vercel 项目即可品牌站 + App + API + Prisma。
# 复制为 .env.local 后仅在本地生效（勿将 .env.local 提交到 Git）。
# Next.js 仅会把以 NEXT_PUBLIC_ 开头的变量打进浏览器端代码。

# --- 数据库（生产必需；在 Vercel Environment Variables 配置）---
# 未配置时 `npm run build` 仍会完成 Next 构建，但会跳过 prisma migrate deploy；
# 配好 DATABASE_URL 后请重新部署一次，让迁移在构建阶段执行。
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require

# --- 可选：演示种子 API（与 prisma/seed 或 /api/seed 说明一致）---
# DEMO_SEED_SECRET=your-long-random-secret

# --- 同源合并后：可不设，/login 内嵌会使用本站 /welcome/login?embed=1 ---
# 若 Vercel 里仍留着旧的 NEXT_PUBLIC_MINIAPP_URL 且指向「本站同一域名」，代码会视为同源合并；
# 为免混淆可直接删除该变量后 Redeploy。
# 若仍用「品牌站 + 独立 App 域名」分仓部署，再填写外域 App origin：
# NEXT_PUBLIC_MINIAPP_URL=https://你的-app.vercel.app

# --- 可选：/welcome/login 的 CSP frame-ancestors 额外允许列表（逗号分隔）---
# FRAME_ANCESTOR_ORIGINS=https://example.com

# --- 可选：canonical / 绝对链接（未设且部署在 Vercel 时 next.config 会用 VERCEL_URL 推断）---
# NEXT_PUBLIC_SITE_URL=https://你的项目.vercel.app
